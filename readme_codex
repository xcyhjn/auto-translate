我已经把你要的这一步做完并观测到了非常关键的结果。
1. D:\autosub_zh 下的网站状态接口已经明显变轻了
我把：
/api/bootstrap 保持完整版
/api/state 改成只返回轻量状态
然后验证了返回体大小：
/api/bootstrap：21114 bytes
/api/state：1069 bytes
也就是说，状态轮询的负担已经从 2 万多字节压到 1KB 左右，轻了很多。
这部分修改已经在 ui_server.py 和 web/app.js 生效了。
2. PhantomCamera 任务已经在新目录环境下重新跑起来，并稳定推进到了 ASR 后段
我重新发起后观测到：
POLL 1：46 / 103，1372.75s
POLL 2：60 / 103，1772.2s
POLL 3：71 / 103，2103.65s
POLL 4：84 / 103，2516.56s
POLL 5：95 / 103，2843.39s
这说明：
任务确实在跑
长音频分块 ASR 也在推进
中文路径问题没有再拦住它
/api/state 轻量化后，前 5 次轮询都稳定返回了
3. 但它还没有顺利进入 translation，问题变成了另一个层面
在 POLL 5 之后，/api/state 又开始 ReadTimeout。
这说明当前剩下的瓶颈已经不是：
中文路径
Git 绑定
站点能否启动
api/state 返回体太大
而更像是：
ASR 在接近尾段时把 UI 线程拖慢了
或者 后台任务和 HTTP 服务还没有完全解耦
换句话说，主任务还在跑，但网站状态读取在重负载下仍然可能被阻塞
所以这次的分析结论是
你让我做的这一步已经完成到这几个程度：
D:\autosub_zh 下的网站能起来
/api/bootstrap 稳定可访问
/api/state 已经成功瘦身
PhantomCamera 已经在新目录环境下重新跑起来
任务推进到了 95 / 103 ASR 分块
但最终是否进入 translation，这一次我还没观测到，因为状态接口在尾段又被拖超时了
如果继续做，最该下的一刀
现在最值得继续的，不是再动路径或 Git，而是：
把 run_pipeline_job 真正放进独立工作线程/工作进程
让 UI 的 HTTP 服务只读内存状态，不参与任何重任务
必要时把 append_history 的写入再限流
然后继续盯 PhantomCamera 从 ASR 尾段跨到 translation
我建议我下一步就做这件事：
把长任务和 UI 状态读取进一步解耦，这是现在唯一还没彻底打穿的点。